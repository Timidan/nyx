# Copy this file outside Git, replace the placeholder, and set mode 600.
# The populated file is mounted only into nyx-web as a Compose secret.
proxy_set_header Authorization "Bearer <nyx-agent-api-bearer-token>";
